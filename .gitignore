# ─── Node ───────────────────────────────────────
node_modules/
npm-debug.log*

# ─── SENSITIVE DATA ──────────────────────────────
# Contains plaintext passwords — NEVER commit this
data/config.private.json

# ─── User uploads (large binary files) ──────────
data/uploads/

# ─── Public config is auto-generated by server ──
# Commit the initial one but exclude local changes
# (optional — remove the next line if you want it tracked)
# data/config.json

# ─── OS / Editor ─────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
*.log
